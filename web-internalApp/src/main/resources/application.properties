app.url=http://localhost:8082/
exApp.url=http://localhost:8081/
server.port=8082

spring.jpa.hibernate.ddl-auto=update
spring.datasource.url=jdbc:mysql://${DB_HOST:localhost}:3306/${DB_NAME:repairservice_db}
spring.datasource.username=${DB_USER:demo}
spring.datasource.password=${DB_PASSWORD:demo123+}
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.datasource.continue-on-error=true

################### JavaMail Configuration ##########################
mail.debug=true
support.email=info@ivl.lt
spring.mail.host=${EMAIL_HOST:localhost}
spring.mail.port=${EMAIL_PORT:1025}
spring.mail.username=${EMAIL_USER:''}
spring.mail.password=${EMAIL_PASSWORD:''}
spring.mail.properties.mail.transport.protocol=smtp
spring.mail.properties.mail.smtps.auth=false
spring.mail.properties.mail.smtps.starttls.enable=false
